#include <stdio.h>

int main() {
    int x,i,j;
    scanf("%d",&x);
    i=x+1;
	while(1)
	{
	    
	    int count=0;
	    for(j=1;j<=i;j++)
	    {
	        if(i%j==0)
	        count++;
	    }
	    
	    if(count==2)
        {printf("%d",i);
	    break;}
	    i++;
	}
	return 0;
}