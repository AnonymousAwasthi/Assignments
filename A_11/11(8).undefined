#include <stdio.h>

int main() {
	int i,j,n,count=1;
	scanf("%d",&n);
	for(i=1;i<=n;i++)
	{ //char c='A';
	    for(j=1;j<=n;j++)
	    {
	        if(j<=i)
	        {printf("%d ",count);
	        count++;}
	        else
	        printf("  ");
	    }
	    printf("\n");
	}
	
	return 0;
}