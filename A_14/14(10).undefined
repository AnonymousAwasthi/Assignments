#include <stdio.h>

int main() {
	void factor(int x)
	{
	    for(int i=2;i<=x;x>1)
	    {
	        if(x%i==0)
	        {
	            printf("%d, ",i);
	            x=x/i;
	        }
	        else
	        i++;
	        
	    }
	}
	factor(56);
	return 0;
}