#include<stdio.h>
int main()
{
    int x,y,z,max;
    scanf("%d%d%d",&x,&y,&z);
    if(x>y)
    max=x;
    else
    max=y;
    if(max>z)
    max=max;
    else
    max=z;
    printf("greatest among %d,%d,%d is %d",x,y,z,max);
    return 0;
}