#include <stdio.h>
int gcd(int a, int b)
{
    if (b == 0)
        return a;
    return gcd(b, a % b); 
    
}
int main() {
	int x;
    scanf("%d",&x);
    if(gcd(x,2)==1)
    printf("Number is odd");
    else
    printf("Number is even");
    return 0;

}