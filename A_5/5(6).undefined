#include<stdio.h>
int main()
{
    int x;
    scanf("%d",&x);
    int count=0;
    while(x!=0)
    {
        x=x/10;
        count++;
    }
    if(count==3)
    printf("Number is a three digit number");
    else 
    printf("number is not a three digit number");
    return 0;
}