#include<stdio.h>
int main()
{
    int x,count=0;
    scanf("%d",&x);
    while(1)
    {
        if(x==0)
        break;
        x=x/10;
        count++;
    }
    printf("%d",count);
    return 0;
}
